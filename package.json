{
  "name": "PropEase-Real-Estate-Management-System",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev --prefix backend\" \"npm:start --prefix frontend\"",
    "start": "concurrently \"npm:start --prefix backend\" \"npm:preview --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "wait-on": "^7.0.1"
  },
  "dependencies": {
    "morgan": "^1.10.1"
  }
}
